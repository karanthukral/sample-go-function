cleanNamespace: true
bucket: {}
packages:
  - name: default
    actions:
      - name: "hello"
        runtime: 'go:default'
        web: false
        webSecure: true
        clean: true
        sequence:
          - abc
        annotations:
          "require-whisk-auth": ""
          "provide-api-key": ""
    shared: false
    clean: false
    annotations:
      "other_annotation": "yolo"
